[package]
name = "s1engine-c"
description = "C FFI bindings for s1engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
s1engine = { workspace = true }
